syntax = "proto3";

package grpc;

option go_package = "github.com/Sistemas-Distribuidos-2022-2/Ejemplo/proto";

message Message {
    string body = 1;
}

service MessageService {
    rpc Intercambio (Message) returns (Message);
}